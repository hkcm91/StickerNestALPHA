/**
 * Poppit Test Widget — Barrel Exports
 *
 * Balloon-popping game for testing rooms and leaderboards.
 *
 * @module runtime/widgets/poppit-test
 * @layer L3
 */

// HTML source is loaded via ?raw import in init.ts
// This widget is HTML-only (no React component)
